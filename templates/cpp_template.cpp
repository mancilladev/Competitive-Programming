#include <bits/stdc++.h>
using namespace std;
#define PB push_back
typedef long long ll;
const ll INF = ll(1e18);

#ifndef ONLINE_JUDGE
    freopen("in", "r", stdin);
#else
    ios_base::sync_with_stdio(0);
    cin.tie(nullptr);
#endif

int main(void) {

    return 0;
}
