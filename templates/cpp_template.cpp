#include <bits/stdc++.h>
#define PB push_back
using namespace std;
typedef long long ll;
const ll INF = ll(1e18);
int N, arr[N+1] {0};

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(nullptr);

    cin >> N;
    for (int i = 0; i < N; ++i)
        cin >> arr[i];

    return 0;
}
